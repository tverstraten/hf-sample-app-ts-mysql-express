import { SystemDescription } from '@tverstraten/hf-model'
import { AbstractMutableDac } from './AbstractMutableDac'

/**
 * NOTE: This file is meant to be extended, add to it as you need. It is only generated if it does not already exist.
 */
export class SystemDescriptionDac extends AbstractMutableDac<SystemDescription> 
{
	// Add custom code as required
}
