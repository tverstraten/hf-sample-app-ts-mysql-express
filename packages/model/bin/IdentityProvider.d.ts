/**
 * the identitity providers we currently support
 */
export declare enum IdentityProvider {
    github = "github",
    google = "google",
    microsoft = "microsoft",
    personalAccessToken = "personalAccessToken"
}
