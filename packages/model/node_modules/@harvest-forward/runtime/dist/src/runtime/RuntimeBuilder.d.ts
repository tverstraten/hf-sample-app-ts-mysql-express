import { Artifact } from '../system/Artifact';
import { System } from '../system/System';
import { SystemComponent } from '../system/SystemComponent';
import { AbstractSingularBuilder } from './AbstractSingularBuilder';
import { BuilderConfiguration } from './BuilderConfiguration';
export declare class RuntimeBuilder extends AbstractSingularBuilder {
    private configuration;
    constructor(system: System, configuration: BuilderConfiguration);
    protected buildInternal(system: System, __component: SystemComponent): Promise<Artifact[]>;
}
//# sourceMappingURL=RuntimeBuilder.d.ts.map