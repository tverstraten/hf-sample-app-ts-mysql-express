# Informational model in typescript (no framework) for {{name}}
# {{description}}

# {{this.name}}
# {{this.description}}
class {{this.name}}_generated:
    def __init__(self{{#each this.properties}}, {{this.name}}Value{{/each}}):
{{#each this.properties}}
        self._{{this.name}} = {{this.name}}Value
{{/each}}

{{#each this.properties}}
	# Get the value of the {{this.name}} property
	# @returns the current value of the {{this.name}} property
    def get_{{this.name}}(self):
        return self._{{this.name}}

	# Set the current value of the {{this.name}} property
	# @param newValue the new value to use for the {{this.name}} property
    def set_{{this.name}}(self, value):
        self._{{this.name}} = value;
	
    # {{this.name}} property
	# {{this.description}}
    {{this.name}} = property(get_{{this.name}}, set_{{this.name}})

{{/each}}


