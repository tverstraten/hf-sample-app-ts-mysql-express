
    private _relPropName: Array<PropType> | undefined;

    get relPropName(): Array<PropType> | undefined {
        return this.getArrayProperty('_relPropName');
    }

    addOneUPropName(newValue: PropType): void {
        return this.addOneArrayProperty('_relPropName', newValue);
    }

    removeOneUPropName(oldValue: PropType): void {
        return this.removeOneArrayProperty('_relPropName', oldValue);
    }

    addManyUPropName(newValues: Array<PropType>): void {
        return this.addManyArrayProperty('_relPropName', newValues);
    }

    removeManyUPropName(oldValues: Array<PropType>): void {
        return this.removeManyArrayProperty('_relPropName', oldValues);
    }

    clearUPropName(): void {
        return this.clearArrayProperty('_relPropName');
    }

    sizeofUPropName(): number {
        return (this._relPropName == null) ? 0 : this._relPropName.length;
    }

    relPropNameHas(entryToCheck: PropType): boolean {
        return (this._relPropName == null) ? false : this._relPropName.includes(entryToCheck);
    }