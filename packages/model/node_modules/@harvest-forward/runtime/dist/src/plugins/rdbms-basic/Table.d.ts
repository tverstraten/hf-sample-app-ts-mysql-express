import { SystemComponent } from '../../system/SystemComponent';
import { Column } from './Column';
import { Constraint } from './Constraint';
import { ForeignKey } from './ForeignKey';
import { RelationalStorage } from './RelationalStorage';
import { TableType } from './TableType';
export declare class Table extends RelationalStorage {
    primaryKey?: Column;
    _foreignKeys: {
        [key: string]: ForeignKey;
    };
    _constraints: {
        [key: string]: Constraint;
    };
    constructor(schemaName: string, tableName: string, description: string, tableType: TableType);
    get foreignKeys(): {
        [key: string]: ForeignKey;
    };
    get constraints(): {
        [key: string]: Constraint;
    };
    addChild(child: SystemComponent): void;
    removeChild(child: SystemComponent): boolean;
}
//# sourceMappingURL=Table.d.ts.map