/**
 * The identity providers we currently support.
 */
export enum IdentityProvider {
	github = 'github',
	google = 'google',
	microsoft = 'microsoft',
	personalAccessToken = 'personalAccessToken',
}
