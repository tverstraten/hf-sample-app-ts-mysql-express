/**
 * NOTE: This file is generated, do not make changes to it.
 */
import { AbstractDac } from '@tverstraten/hf-dal'
import { FindResults } from '@tverstraten/hf-dal'

/**
 * A base point of inheritance for data oriented services.
 * NOTE: This class is generated, do not make changes to it.
 */
export abstract class AbstractService<T> {
	typedDac: AbstractDac<T>

	constructor(dac: AbstractDac<T>) {
		this.typedDac = dac
	}

	async findBy(where: string, params: any[] = [], orderBy?: string, page = 1, pageSize = 20): Promise<FindResults<T>> {
		return this.typedDac.findBy(where, params, orderBy, page, pageSize)
	}

	async findOneById(id: number): Promise<T | null> {
		return this.typedDac.findOneById(id)
	}
}
