/**
 * Used in getting the generic in the derivations of the object that crud operations are performed on.
 * NOTE: This file is generated, do not make changes to it.
 *
 * @param T - The type of object that is constructed.
 */
export interface NoParamConstructor<T> {
	new (): T
}
