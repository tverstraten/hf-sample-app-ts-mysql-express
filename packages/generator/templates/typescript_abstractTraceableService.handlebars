/**
 * NOTE: This file is generated, do not make changes to it.
 */
import { AbstractTraceableDac } from '@tverstraten/hf-dal'
import { AbstractService } from './AbstractService'
import { Traceable } from '@tverstraten/hf-model'

/**
 * A base point of inheritance for services that work with persistent data whose lineage can be traced.
 * NOTE: This class is generated, do not make changes to it.
 */
export abstract class AbstractTraceableService<T extends Traceable> extends AbstractService<T> {
	traceableDac: AbstractTraceableDac<T>

	constructor(dac: AbstractTraceableDac<T>) {
		super(dac)
		this.traceableDac = dac
	}

	async create(newItem: T): Promise<boolean> {
		return this.traceableDac.create(newItem)
	}

	async createAndReturn(newItem: T): Promise<T> {
		return this.traceableDac.createAndReturn(newItem)
	}
}
